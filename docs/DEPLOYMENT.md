# Deployment

Install the .NET 8 SDK to build and the .NET 8 Windows Desktop Runtime to run. Install the service with `sc.exe create "Backup Manager Service" binPath= "..." start= auto` after publishing.
